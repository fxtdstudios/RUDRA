# ── Trained weights & data (huge — never commit) ─────────────────────────────
hdrdata/checkpoints/
hdrdata/**/*.npz
hdrdata/*pairs*/
hdrdata/*cache*/
hdrdata/Source_HDR/
*.safetensors
*.pth
*.ckpt
*.npz

# Source HDR / image assets
*.exr
*.hdr
*.tif
*.tiff
*.mxf

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# ── Env / IDE / OS ────────────────────────────────────────────────────────────
.venv/
venv/
env/
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Logs / scratch ────────────────────────────────────────────────────────────
*.log
*.jsonl
outputs/
tmp/

# ── Session scratch (2026-08-22) ─────────────────────────────────────────────
hf_upload/
_to_delete/
.keep_running
training/input.jpg

# ── Docs stay local: ignore all markdown except README.md ────────────────────
*.md
# Root README only -- an anchored pattern, so a docs/README.md or any other
# nested markdown cannot slip back in through the negation.
!/README.md

# Audit, review and status reports stay local too. These were written as HTML so
# they slipped past the *.md rule and stayed tracked; the same content belongs in
# the code's own docstrings, not in dated files that go stale in the repo.
AUDIT_*.html
*_REVIEW_*.html
COMPETITIVE_*.html
*_AUDIT_*.html
