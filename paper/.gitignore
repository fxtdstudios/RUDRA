# LaTeX build litter. _body.tex and _abstract.tex ARE committed -- they are
# generated by mdtotex.py but are what gets uploaded to arXiv.
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
rudra-arxiv.tar.gz
arxiv/

# main.pdf IS committed: the README links it as the paper, so a clone that
# cannot build LaTeX still gets the document. Rebuild it with build.sh.
