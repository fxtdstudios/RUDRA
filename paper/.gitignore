# LaTeX build litter. The .tex sources and refs.bib ARE committed, and so is
# main.bbl: arXiv runs latex but not bibtex, so the bibliography has to travel
# with the upload.
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.blg
rudra-arxiv.tar.gz
arxiv/

# main.pdf IS committed: the README links it as the paper, so a clone that
# cannot build LaTeX still gets the document. Rebuild it with build.sh.
