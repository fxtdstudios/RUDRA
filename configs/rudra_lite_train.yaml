name: rudra_lite_train
version: 0.3
mode: rudra_lite
model_type: flux
batch_size: 4
grad_accum: 4
learning_rate: 1.0e-4
max_steps: 80000
validate_every: 1000
checkpoint_every: 5000
outputs:
  color_space: ACEScg
  output_domain: scene_linear
